import React from 'react';

const NotMatch = () => {
    return (
        <div style={{textAlign:'center',color:'red'}}>
            <h1>page not found,pleace try again</h1>
            <h4>ERROR 400</h4>
        </div>
    );
};

export default NotMatch;